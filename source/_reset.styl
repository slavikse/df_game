// normalize 4.2.0 + reset.css 2.0 + reboot.css + правки и добавки

/**
 * 1. prevent adjustments of font size after orientation changes in IE and iOS
 * 2. default tap highlight iOS
 * 3. IE11 & Edge
 */
html {
  font-family: sans-serif;
  font-size: 16px;
  box-sizing: border-box;
  -ms-text-size-adjust: 100%; /* 1 */
  -webkit-text-size-adjust: 100%; /* 1 */
  -webkit-tap-highlight-color: transparent; /* 2 */
  -ms-overflow-style: scrollbar; /* 3 */
}

*,
*::before,
*::after {
  box-sizing: inherit;
}

body {
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
  font-size: 1rem;
  line-height: 1.5;
  color: #373a3c;
  background-color: #fff;
  text-rendering: optimizeSpeed;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

body,
p,
h1,
h2,
h3,
h4,
h5,
h6 {
  margin: 0;
}

/**
 * 1. FF & tab focus Chrome
 * 2. remove the gray on active links in IE 10
 * 3. iOS 8+, safari 8+
 */
a {
  color: inherit;
  text-decoration: none;
  outline: 0; /* 1 */
  background-color: transparent; /* 2 */
  -webkit-text-decoration-skip: objects; /* 3 */
}

/**
 * 1. remove the border on images inside links in IE 10-
 */
img {
  max-width: 100%;
  height: auto;
  vertical-align: middle;
  border-style: none; /* 1 */
}

/**
 * remove the inheritance of text transform in FF
 */
button,
select {
  cursor: pointer;
  text-transform: none; /* 1 */
}

/**
 * 1. remove the margin in FF and Safari
 * 2. change font properties to `inherit` in all browsers
 * 3. iOS adds rounded borders by default
 * 4. FF
 */
button,
input,
select,
textarea {
  margin: 0; /* 1 */
  padding: 0;
  font: inherit; /* 2 */
  line-height: inherit;
  border: 0;
  outline: 0;
  border-radius: 0; /* 3 */
  background-color: transparent; /* 4 */
}

/**
 * remove the inner border and padding in FF
 */
button::-moz-focus-inner,
[type="button"]::-moz-focus-inner,
[type="reset"]::-moz-focus-inner,
[type="submit"]::-moz-focus-inner {
  border: 0;
  padding: 0;
}

/**
 * avoid 300ms click delay on touch devices
 */
a,
button,
input,
label,
select,
textarea {
  touch-action: manipulation;
}

ol,
ul {
  margin: 0;
  padding: 0;
  list-style: none;
}

/**
 * 1. remove the default vertical scrollbar in IE
 */
textarea {
  overflow: auto; /* 1 */
  resize: vertical;
}

table {
  border-collapse: collapse;
  border-spacing: 0;
}

/**
 * correct display in IE
 */
main {
  display: block;
}

/**
 * add the correct display in IE
 */
[hidden], /* IE 10- */
template {
  display: none;
}

/**
 * remove the padding in IE 10-
 */
[type="checkbox"],
[type="radio"] {
  padding: 0;
}
